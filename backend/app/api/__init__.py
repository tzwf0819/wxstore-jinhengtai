
from fastapi import APIRouter

# This file defines the main API router.
# Individual endpoint routers are now included directly in the main application file (main.py)
# to prevent circular dependencies.

api_router = APIRouter()
