'''<view class="container">
  <!-- 轮播图 -->
  <block wx:if="{{banners && banners.length > 0}}">
    <swiper class="banner-swiper" indicator-dots autoplay circular>
      <block wx:for="{{banners}}" wx:key="id">
        <swiper-item>
          <image wx:if="{{item.image_url}}" src="{{item.image_url}}" class="slide-image" mode="aspectFill"/>
        </swiper-item>
      </block>
    </swiper>
  </block>

  <!-- 热门商品 -->
  <view class="section">
    <view class="section-title">热门推荐</view>
    <view class="product-grid">
      <view wx:if="{{loading && products.length === 0}}" class="loading-tip">加载中...</view>
      <view wx:if="{{!loading && products.length === 0}}" class="empty-tip">暂无热门商品</view>
      <block wx:for="{{products}}" wx:key="id">
        <view class="product-item" bindtap="goToProductDetail" data-id="{{item.id}}">
          <image wx:if="{{item.image_url}}" class="product-image" src="{{item.image_url}}" mode="aspectFill"></image>
          <image wx:else class="product-image" src="/static/images/product_placeholder.png" mode="aspectFill"></image>
          <view class="product-info">
            <view class="product-name">{{item.name}}</view>
            <view class="product-price">¥{{item.price}}</view>
          </view>
        </view>
      </block>
    </view>
  </view>
</view>
''